{
  "name": "copyurl",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:integration": "jest --testMatch='**/tests/integration/**/*.test.js'",
    "test:unit": "jest --testMatch='**/tests/*.test.js' --ignore='**/tests/integration/**'",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.22.0",
    "babel-jest": "^29.6.0",
    "jest": "^29.6.0",
    "jsdom": "^22.1.0"
  }
}
